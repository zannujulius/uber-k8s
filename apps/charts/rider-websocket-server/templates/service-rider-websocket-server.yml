apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.riderWebsocketServer.releaseName }}-service
  labels:
    app: {{ .Values.riderWebsocketServer.releaseName }}-app
spec:
  type: ClusterIP
  selector:
    app: {{ .Values.riderWebsocketServer.releaseName }}-app
  ports:
    - protocol: TCP
      port: {{ .Values.riderWebsocketServer.service.port }}
      targetPort: {{ .Values.riderWebsocketServer.service.targetPort }}